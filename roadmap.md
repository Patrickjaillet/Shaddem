# ShaderDemo — C# Port Roadmap

**Copyright (c) 2026 Patrick JAILLET**

Source project: `OPENGL_SHADERSDEMO` (Python / pygame / moderngl / PyOpenGL / pyimgui), located at `C:\Users\Patrick\Desktop\OPENGL_SHADERSDEMO` — this is the reference for every 1:1 port and the source of reusable assets (`shaders/`, `audio/`, `textures/`, `images/`, `assets/` (mp4 backgrounds), `models/cube.obj`). Only `shaders/*.glsl` (renamed from `.hlsl`, see §3) has been copied into this repo so far; audio/textures/video/model assets stay in the source tree, loaded from there by full path when exercised (e.g. by `--test-full`), to avoid bloating this repo with unused multi-hundred-MB binaries.
Target project: 100% C# / .NET 8, **Silk.NET** (windowing + OpenGL bindings) + **ImGui.NET** (Dear ImGui bindings)

> **This file is a living document.** Every code change, every completed task, every new
> decision must be reflected here **in the same commit/session** that makes the change.
> Never let this roadmap drift out of sync with the actual code.

---

## 0. Project rules (non-negotiable)

- [ ] No comments of any kind in source code (no `//`, no `/* */`, no XML doc comments)
- [ ] All UI text, log messages, file names, shader names, exceptions: **English only**
- [ ] Every `.cs` file starts with a single header line:
  `// Copyright (c) 2026 Patrick JAILLET` (this is the only tolerated "comment" — copyright header only)
- [ ] `roadmap.md` updated at every modification (check boxes, add rows, log decisions in §12)
- [ ] Target framework: `.NET 8` (LTS), `C# 12`
- [ ] Solution builds a single self-contained Windows executable (`dotnet publish`), no PyInstaller-style bloat
- [ ] Naming: PascalCase for types/methods, camelCase for locals/fields (`_field` for private)

---

## 1. Solution & project setup

- [x] Create solution `ShaderDemo.sln`
- [x] Project `ShaderDemo.Core` (engine, rendering, shaders, timeline, audio, video — class library)
- [x] Project `ShaderDemo.App` (entry point, window, ImGui UI — executable)
- [x] Add NuGet packages: `Silk.NET.Windowing`, `Silk.NET.OpenGL`, `Silk.NET.Input`, `ImGui.NET`, `StbImageSharp` (textures), `NAudio` (audio decode/analysis) — `System.Text.Json` is built into .NET 8, no explicit package needed
- [x] `.editorconfig` enforcing naming rules (no-comments is a process rule, not analyzer-enforceable — see §0)
- [x] `.gitignore` (bin/, obj/, *.user)
- [x] CI-less local build script `build.ps1` replacing `compile.py`
- [x] App icon migrated (`icon.ico`)
- [x] `global.json` pinning SDK `8.0.100`, `rollForward: latestMinor`
- [x] Minimal `Program.cs` entry point proving the solution structure (prints app name/version/copyright) — windowing bootstrap moved to §2, not implemented yet

---

## 2. Core rendering engine (from `main.py`, `engine.py`, `render_pipeline.py`)

### 2.0 Foundation (implemented)

- [x] `GlWindow` class: Silk.NET window creation, GL context, resize/close handling, mouse input (replaces pygame window init) — `Rendering/GlWindow.cs`
- [x] `GlContext` wrapper around `Silk.NET.OpenGL.GL` (replaces `moderngl.Context` / `PyOpenGL`) — `Rendering/GlContext.cs`
- [x] `Framebuffer` class: FBO + color texture creation, `Create`/`CreateScaled` factory methods (replaces `create_fbo` / `create_scaled_fbo`) — `Rendering/Framebuffer.cs`
- [x] `FullscreenQuad`: VAO/VBO for full-screen triangle strip (replaces `create_quad`) — `Rendering/FullscreenQuad.cs`
- [x] `ShaderProgram`: compile/link vertex+fragment shader, uniform location cache, typed `SetUniform` overloads (replaces raw `ctx.program(...)` + `set_uniform`) — `Rendering/ShaderProgram.cs`
- [x] `BlendMode` enum: Replace/Add/Multiply/Screen/Overlay/Normal/Difference/Exclusion (replaces the int blend_mode convention in `Layer`) — `Rendering/BlendMode.cs`
- [x] `Layer` model: shader ref, blend mode, opacity, enabled state (replaces `Layer` in `engine.py`) — `Rendering/Layer.cs`
- [x] `EffectParams`: strongly-typed container for all `customXxx` uniforms (63 fields, 1:1 with the `CUSTOM_*` fields discovered in `engine.py`'s fallback `ShaderSettings` and the uniform list in `shaders.py::get_fragment_wrapper`), with an `Apply(ShaderProgram)` helper — `Rendering/EffectParams.cs`
- [x] `ShaderWrapper`: builds the full fragment shader source (uniform block + HLSL-style helpers `saturate`/`mul`/`hueShift` + user `mainImage` code + `main()` bridge), 1:1 with `shaders.py::get_fragment_wrapper` (replaces `hlsl_to_glsl`/wrapper string) — `Rendering/ShaderWrapper.cs`
- [x] `BuiltinShaders`: vertex passthrough, texture blit, layer-blend fragment sources (replaces `VERTEX_SHADER_SIMPLE` / `FRAGMENT_SHADER_TEXTURE` / `FRAGMENT_SHADER_BLEND`) — `Rendering/BuiltinShaders.cs`
- [x] `RenderPipeline`: scene FBO, per-layer FBO, two compose FBOs; `RenderShaderPass` (sets `iResolution`/`iTime`/`iOffset`/`iMouse` + all effect uniforms, draws the quad), `BlendLayer` (layer compositing via `BlendMode`), `PresentToScreen` (replaces the FBO/quad plumbing in `render_pipeline.py` + the base-layer part of `render_frame`) — `Rendering/RenderPipeline.cs`
- [x] `ShaderManager`: holds the shader dictionary, `Layers`, `EffectParams`, current shader index, elapsed time, per-frame `Update`/`RenderFrame` orchestrating the pipeline + layer loop (replaces `ShaderManager` in `engine.py`) — `Rendering/ShaderManager.cs`
- [x] Main loop wiring in `Program.cs`: `GlWindow` events (`Load`/`UpdateFrame`/`RenderFrame`/`Resized`/`Closing`) drive `ShaderManager`; a small built-in test-pattern shader proves the pipeline renders end-to-end
- [x] Per-frame `iTime`/`iResolution`/`iMouse`/`iOffset` uniforms updated every frame via `RenderPipeline.RenderShaderPass`

### 2.1 Still open from `engine.py` / `render_pipeline.py` (not yet ported — tracked here so nothing gets lost)

- [x] Shader file loading from the `shaders/` folder (`load_shaders`, `create_shader_program`) — `ShaderLoader.LoadFromDirectory` reads every `*.hlsl`, strips non-ASCII bytes (source files carried French comments that corrupted the native GLSL compiler — see §12), runs it through `HlslToGlslConverter`, and registers it via `ShaderManager.RegisterShader`; wired into `Program.cs` (loads from `<exe>/shaders`, falls back to the built-in test pattern if none found) with Left/Right arrow keys to switch — `Rendering/ShaderLoader.cs`. All 33 `.hlsl` files from the Python project's `shaders/` folder were copied in as-is and 32/33 compile; `black_hole.hlsl` fails with a genuine GLSL 330 strictness error (implicit `vec3`→`float` narrowing) in the shader's own source — left as a known per-shader issue, not an infra bug
- [x] `HlslToGlslConverter` utility ported 1:1 from `shaders.py::hlsl_to_glsl` — `Rendering/HlslToGlslConverter.cs` (also closes the §3 item below)
- [x] Audio-reactive modulation of shake/strobe/RGB-split/kick/scale — `Audio/AudioReactivity.cs` (ported in the §6 session; **rotation-speed excluded** because it depends on `treble_data`, which Python initializes but never actually populates anywhere — confirmed dead code upstream, nothing to port)
- [x] `customAudioKick` uniform wiring — `RenderPipeline.RenderShaderPass` sets it from `AudioReactivity.Compute` every pass (§6 session)
- [x] Bloom pass (downsample/blur/combine) — `Rendering/PostEffectsPipeline.cs::RenderBloom` + `BuiltinShaders.FragmentBloomDownsample/Blur/Combine`, ported 1:1 from `render_bloom`/the three bloom fragment shaders in `shaders.py`. Wired into `ShaderManager.RenderFrame`, gated on `EffectParams.Bloom > 0`, applied after the layer chain + particles + 3D model overlay exactly where Python's `render_frame` step 2.6 does it
- [x] Transition system between two shaders (Wipe/Fade/Zoom/Pixelize) — `PostEffectsPipeline.RenderTransition` + `BuiltinShaders.FragmentTransition`, ported 1:1 from `render_transition`. `ShaderManager.StartTransition(newIndex, duration, type)` is a new explicit trigger (Python enters transition state via `main.py`'s shader-switch-interval logic, which this port doesn't have a direct equivalent of yet — see the Timeline-driven note in §5); exposed as "Transition to Next/Selected" buttons in `EffectsPanel` alongside the existing instant Previous/Next
- [x] Motion blur accumulation (ping/pong FBOs) — `PostEffectsPipeline.RenderMotionBlur` + `BuiltinShaders.FragmentMotionBlur`
- [x] Datamosh accumulation pass — `PostEffectsPipeline.RenderDatamosh` + `BuiltinShaders.FragmentDatamosh`
- [x] Feedback pass (zoom/rotate/opacity accumulation) — `PostEffectsPipeline.RenderFeedback` + `BuiltinShaders.FragmentFeedback`. Datamosh/Feedback/MotionBlur are mutually exclusive by priority (datamosh > feedback > motion blur), matching the `elif` chain in `main.py` exactly — `ShaderManager.RenderAccumulationEffects`, using two dedicated ping-pong framebuffers (`_accumPing`/`_accumPong`)
- [x] Particle system — **deviation, logged here deliberately**: Python's `ParticleMixin` simulates particles on the GPU via a `#version 430` compute shader + SSBOs (`buf_pos`/`buf_vel`, `bind_to_storage_buffer`). `Rendering/ParticleSystem.cs` instead simulates on the CPU (plain C# arrays, re-uploaded to a dynamic VBO every frame via `BufferData`) and keeps the same GLSL vertex/fragment rendering shaders (`gl_PointSize`, circular point discard) verbatim. At the ~2000-particle scale this project uses, CPU simulation costs nothing measurable and avoids building compute-shader/SSBO infrastructure (a new GL feature surface) for this one feature. Revisit only if particle counts grow into the tens of thousands. Physics formulas (velocity integration, kick-driven radial push, distance-15 respawn) ported 1:1
- [x] 3D model support — OBJ loading (`Rendering/ObjModel.cs`, a small hand-written parser: `v`/`vt`/`vn`/`f` lines, triangle-fans quad faces exactly like `load_obj_model`, not `AssimpNet` — a full model-import library was unjustified for parsing the simple ASCII OBJ format this project actually uses), wireframe rendering (`Rendering/ModelRenderer.cs`, `glPolygonMode(FrontAndBack, Line)` + `use_solid_color` uniform, matching `render_3d_model_overlay`). **Not ported**: `model_presets.json` (a second, separate presets file for model transform+texture combos) — the general `EffectParams` presets system below was judged higher-value for the time available; `Gui/Model3DPanel.cs` exposes direct transform/light/wireframe controls instead. Verified against the real `models/cube.obj` from the source project (36 vertices = 6 quad faces × 2 triangles, correct) and confirmed rendering correctly via screenshot: proper perspective, per-face shading, positioned inside the shader background, composited before bloom exactly like Python
- [x] Audio spectrum/waveform overlay with trail decay — `Audio/AudioAnalyzer.AnalyzeFull` (new: per-frame-at-60fps Hann-windowed FFT → 64-band spectrum + raw waveform + bass/treble, ported from `engine_mixins.py::AudioAnalysisMixin.analyze_audio`) + `Rendering/AudioOverlay.cs` (spectrum/waveform bar rendering + ping-pong fade trail, ported from `render_audio_overlay`) + `PostEffectsPipeline.RenderSpectrumBar`/`RenderWaveformBar`/`RenderFade` + the corresponding fragment shaders. **Deviation**: `NAudio.Dsp.FastFourierTransform` only supports power-of-two sizes, but `samples_per_frame` (sample rate ÷ 60) usually isn't a power of two (e.g. 735 at 44.1kHz) — each frame is zero-padded up to the next power of two (1024) before the FFT, which shifts the frequency-bin resolution slightly from numpy's exact-size FFT. Visually equivalent, not bit-exact; flag if exact bin alignment ever matters. Verified against the real ~5 min WAV used throughout this project: 17918 frames analyzed (matches 298.6s × 60fps) with no crash
- [x] Text overlay system — `Rendering/TextOverlay.cs` + `PostEffectsPipeline.RenderOverlay` + `BuiltinShaders.FragmentOverlay` (glitch/wave/rainbow/typewriter/fade, ported 1:1 from `render_text_overlay`'s fragment shader and alpha/scroll math). **Scoped to the Timeline-driven path only** (`ClipType.Text` clips via `TimelineEngine.GetActiveClips`) — Python's separate `manual_text_active`-driven path (direct UI text entry, independent of the timeline) was not ported since the timeline path already covers the same rendering machinery and is the more "automatic"/integrated mechanism already built in this port. Verified end-to-end via screenshot: text renders correctly centered, right-side-up, composited on top of the 3D model + bloom + shader background
- [x] Presets system — `Settings/PresetsService.cs` (`Load`/`Save`/`CreatePreset`/`ApplyPreset`/`GenerateRandomPreset`, ported 1:1 from `engine.py`'s preset functions including the exact theme-based random generator: retro/art/geo/trippy/dark with the same probabilities and value ranges) + `Gui/PresetsPanel.cs`. **Scoped to `EffectParams` only** — Python's `create_preset`/`apply_preset` also snapshot `MUSIC_VOLUME`/`AUDIO_REACTIVE`, which this port leaves out of presets (they're session-level audio settings, not part of a visual "look"); note if that parity turns out to matter. Not ported: the separate `model_presets.json` (see 3D model item above)
- [x] Live-coding shader path — `ShaderManager.CompileLiveShader` (converts + wraps + compiles a live `mainImage` source into a swappable `ShaderProgram`, matching `create_shader_program_from_string`; `IsLiveCodingActive` toggle mirrors `is_live_coding_active`) + `Gui/LiveCodingPanel.cs`, a plain `ImGui.InputTextMultiline` + Compile button. **No syntax highlighting** — the source project doesn't have any either (§9 flags that as a possible future Enhancement, unrelated to this item), and a real code-editor widget (§9's "Tab: Code editor", still unbuilt) was explicitly out of scope for unblocking this specific feature. Verified: compiled a live shader successfully in the integration test
- [x] Multi-window / secondary output support — `Rendering/SecondaryWindow.cs`, a second `Silk.NET` window created with `WindowOptions.SharedContext` pointing at the primary window's GL context (replaces Python's "experimental" pygame `_sdl2` window, built properly on Silk.NET as the roadmap note originally asked for), manually pumped via `IWindow.DoEvents()`/`SwapBuffers()` from inside the primary window's render callback (Silk.NET's supported pattern for a secondary window alongside a primary `.Run()` loop) and blitting `ShaderManager.LastComposedFrame` each frame. `Gui/WindowPanel.cs` has Open/Close buttons. This was the highest-risk item in this batch (shared GL context across two native windows, manual event pump) — verified by actually opening it, rendering 30 frames through it, and closing it again inside the same run as every other feature in this list, with zero crashes and zero stderr output

---

## 3. Shader system (from `modules/shaders.py`)

- [x] Fragment shader wrapper generator (`ShaderWrapper.Wrap`): builds `#version 330` header + all `uniform` declarations + full post-processing `main()` body (all ~50 distortion/color/glitch passes), 1:1 with `shaders.py::get_fragment_wrapper` — `Rendering/ShaderWrapper.cs`
- [x] Uniform registry: strongly-typed `EffectParams` class mapped 1:1 to the Python `CUSTOM_*` settings fields, with `Apply(ShaderProgram)` — `Rendering/EffectParams.cs`
- [x] `HlslToGlslConverter` utility (kept for compatibility with pasted Shadertoy/HLSL-ish snippets): `float2/float3/float4 → vec2/vec3/vec4`, `lerp → mix`, `frac → fract`, `atan2 → atan`, strip `static`/`const` on vector types — `Rendering/HlslToGlslConverter.cs`
- [x] Shader hot-reload: watch `shaders/` folder, recompile on file change (`FileSystemWatcher`) — **Enhancement**: was manual reload in Python version. `Rendering/ShaderHotReloadWatcher.cs`: `FileSystemWatcher` on `*.glsl`/`*.hlsl`, `NotifyFilters.LastWrite`; the watcher's `Changed` event fires on a background thread, so it only enqueues the changed path into a lock-guarded dedup set — the actual `ShaderLoader.LoadFile` (real GL calls) runs from `ProcessPending`, called from `Program.cs`'s `UpdateFrame` on the main/GL thread, matching this project's existing GL-thread-affinity pattern. Verified for real: started the app, appended a line to a shader file **on disk** while it was running, confirmed the log showed `[hot-reload] Loaded shader: art.glsl` and the app kept running with no crash
- [x] Shader compile error reporting surfaced in UI (log panel with GLSL compiler output) — `Rendering/ShaderErrorLog.cs` (a `Dictionary<name, message>`, written to by `ShaderLoader.LoadFile` on both success (clears) and failure (sets)) + `Gui/ShaderErrorsPanel.cs`, wired into `EffectsPanel` as a "Shader Errors (n)" section showing the live count in the header. **Line numbers**: not attempted — the GLSL compiler's own info-log text (already surfaced verbatim) includes line numbers relative to the generated/wrapped source, not the user's original file, and mapping one to the other would need tracking line offsets through `ShaderWrapper`/`HlslToGlslConverter`; out of scope here, the raw compiler message is still shown in full
- [x] Migrate all `.hlsl` shader source files as-is (content is already GLSL-flavoured, only extension is misleading) — rename extension to `.glsl` during port — **Enhancement**. All 33 files in `shaders/` renamed `.hlsl` → `.glsl` (content untouched, confirmed still 32/33 compiling exactly as before — `black_hole.glsl`'s pre-existing GLSL-strictness bug is unrelated to the rename). `ShaderLoader`/`ShaderHotReloadWatcher`/the `.csproj` copy-glob all updated to look for `*.glsl` (kept `*.hlsl` as a secondary pattern too, so a pasted/legacy `.hlsl` file still loads — not a hard requirement, just cheap to keep)
- [x] Snippet library (Palette IQ, Rotation2D, etc.) ported as a static `Snippets` dictionary insertable in the code editor — `Rendering/Snippets.cs`, the same 4 snippets from `main.py`'s `self.snippets` (Palette (IQ), Rotation 2D, Noise 2D (Simple), Raymarching Loop — including that last one's original English inline comments, which were worth keeping since they're user-facing example code explaining what to type, not this project's own implementation comments governed by the §0 no-comments rule). `Gui/LiveCodingPanel.cs` gained a snippet picker + Insert button that appends the chosen snippet to the live-coding text buffer

---

## 4. Post-processing effects parity (from `settings.py` `CUSTOM_*` fields)

Port every effect uniform/control 1:1, grouped by category. **Shader-side status**: all uniforms below are declared, applied in the shader `main()` post-processing chain (`ShaderWrapper.cs`), and exposed on `EffectParams` (§3).

- [x] Color: brightness, contrast, saturation, tint color/intensity, hue shift, invert, solarize, sepia, posterize — sliders in `EffectsPanel.DrawEffectSliders`'s "Color & Light"/"Color Grading" groups
- [x] Distortion: fisheye, wave, mirror, rotation/rotation speed, vortex, swirl, scale, pixelate, mosaic — "Geometry" group
- [x] Glitch/retro: glitch, glitch hard, datamosh, RGB split, chromatic aberration, VHS, scanlines, noise — "Glitch & Retro" group
- [x] Stylize: edge detect, crosshatch, dither, halftone, night vision, thermal — "Artistic" group (Edge Detect/Crosshatch/Dither/Halftone) + "Color Grading" group (Night Vision/Thermal)
- [x] Camera/feel: shake, strobe, vignette, motion blur, feedback (opacity/scale/rotation), bloom — "Motion" group + Bloom/Strobe in "Color & Light"; all wired end-to-end into real render passes (§2.1), not just sliders that no-op
- [x] Particles system: active flag, size, color, count — "Particle System" group. **Deviation**: CPU-simulated `GL_POINTS` (`Rendering/ParticleSystem.cs`), not an instanced quad renderer or the Python version's GPU compute shader — see the full reasoning already logged in §2.1's particle-system entry
- [x] Audio-reactive kick intensity parameter wired to bass envelope — `AudioReactivity.Compute`'s `Kick` output → `customAudioKick` uniform (§2.1/§6)

---

## 5. Timeline & automation (from `timeline.py`, `modules/gui/tabs_timeline.py`, `automation.json`)

- [x] `TimelineEngine`: clip model, `ClipType` enum, playhead, active/inactive state per time (replaces `timeline.py`) — `Timeline/ClipType.cs`, `Timeline/TimelineClip.cs`, `Timeline/TimelineEngine.cs`. `TimelineClip.Params` is a `Dictionary<string, object?>` mirroring Python's `**kwargs` freeform per-clip data (deliberately not a fixed schema — the source data genuinely varies by clip type); `TryGetDouble`/`TryGetDoubleArray`/`GetString` read through both raw CLR values and post-deserialize `JsonElement`s transparently. `GetActiveClips(time)` replaces `get_active_events`
- [x] `Automation` curve model: keyframe list `(time, value)` with interpolation — folded into `TimelineEngine`/`TimelineClip` rather than a separate class, because that's what the Python source actually does: `start_value`/`end_value`/`easing` are just params on an `EFFECT` clip (`calculate_interpolated_value` in `main.py`), there is no distinct automation-curve type to port 1:1. `TimelineEngine.ApplyEffects` reproduces the 4 easing curves (`ease_in`/`ease_out`/`smooth`/`smoother`) and scalar/vector lerp exactly
- [x] Timeline data persistence — **decision made: JSON, not SQLite** (see §12) — `Timeline/TimelinePersistence.cs`, same `System.Text.Json` pattern as `settings.json`/`layers.json`
- [ ] Timeline UI panel (ImGui): clip list, drag/resize clips, playhead scrubbing (replaces `tabs_timeline.py`, 1374 lines — biggest single porting effort). **Partial**: `Gui/TimelinePanel.cs` ships a functional but simplified panel — add-clip form, flat clip list with Remove, Undo/Redo/Save/Load, Enable Timeline toggle, playhead readout. What's **not** built: the graphical drag-to-resize/move clip bars and playhead scrubbing widget (a custom ImGui canvas draw), track lanes, mute/solo, markers UI (the `TimelineMarker` model and persistence exist, just no UI yet). Revisit if the workflow needs visual clip manipulation rather than the form-based editing shipped here
- [x] Shader-switch-on-interval logic — **superseded, not ported as originally scoped**: the Python `SHADER_SWITCH_INTERVAL` auto-advance is a separate, simpler mechanism than the timeline; since the timeline's own `ClipType.Shader` clips already select the active shader by time window (`TimelineEngine.ApplyShader`, wired into `Program.cs`'s `UpdateFrame` when `timeline.Active`), a shader-switch-on-interval clip is just an `EFFECT`-free sequence of adjacent `Shader` clips — no separate scheduler was needed
- [x] Undo/redo for timeline edits — this was actually **already present** in the Python `Timeline` class (`save_state`/`undo`/`redo`, 50-entry history), not a C#-only enhancement as originally labeled here; ported as `TimelineEngine.Undo`/`Redo` with the same 50-entry cap

---

## 6. Audio (from `modules/audio.py`)

- [x] `AudioAnalyzer`: WAV loading + FFT bass envelope extraction (replaces `analyze_audio_file`) — `Audio/AudioAnalyzer.cs`. **Decision**: used `NAudio.Dsp.FastFourierTransform` (already ships inside the `NAudio` package already referenced) instead of adding `MathNet.Numerics` as a second math dependency — same windowed-STFT algorithm as the Python version (1024-sample window, 512 hop, 20–150Hz band average magnitude, normalized to max=1), read via `AudioFileReader` (handles bit-depth conversion, matches Python's raw-PCM approach since normalization makes the scale irrelevant). Verified against a real ~5 min WAV from the source project (not copied into this repo — see §12 asset policy): analyzed in 1.5s, 27996 envelope frames, values sanely bounded [0,1]
- [x] `AudioPlayer` (`Audio/AudioPlayer.cs`, not itemized separately in the original checklist but required to make `MUSIC_VOLUME`/playback real): `NAudio.WaveOutEvent` + `AudioFileReader`, `Play`/`Stop`/`SetVolume`. Verified `IsPlaying` transitions correctly through a real Play → wait → Stop cycle
- [x] Audio-reactive modulation (shake/strobe/RGB-split/scale/kick) — `Audio/AudioReactivity.cs`, ported 1:1 from `engine.py::render_shader`'s inline math (including the no-music fallback: constant tremble for shake, sine-wave strobe), computed fresh per render pass (base + each layer) so shake jitter is independent per pass exactly like the Python `np.random.uniform` call inside the same function. This closes the §2.1 backlog item "Audio-reactive modulation of shake/strobe/RGB-split/rotation-speed/kick/scale" — **rotation-speed excluded**: it depends on `treble_data`, which is initialized to `None` and never actually populated anywhere in the Python source (dead code upstream, confirmed by grep) — not ported since there's nothing to port
- [x] `customAudioKick` uniform wiring (§2.1 backlog item) — closed: `RenderPipeline.RenderShaderPass` now sets it from `AudioReactivity.Compute`
- [ ] Extend beyond `.wav`-only input: support MP3/OGG via NAudio decoders — **Enhancement**
- [ ] Real-time audio input analysis (live mic/line-in FFT), not just pre-rendered file analysis — **Enhancement**
- [ ] Multi-band envelope (bass/mid/treble) instead of bass-only — **Enhancement** (would also finally make the rotation-speed modulation above meaningful)
- [x] Volume control wired to playback (`MUSIC_VOLUME`) — `Gui/AudioPanel.cs` volume slider calls `AudioPlayer.SetVolume`; `AppSettings.MusicVolume` persists it (already existed from §10)

---

## 7. Video export & recording (from `modules/video.py`, recorder logic in `editor_mixins.py`)

- [x] `FfmpegExporter`: build ffmpeg argument list for MP4 (libx264) and GIF (palette-based) export, same flags/quality (CRF 18, preset medium) as Python version — `Export/FfmpegExporter.cs`, ported 1:1 from `video.py::get_ffmpeg_command`. Verified by direct invocation with representative inputs (with/without audio, GIF vs MP4) and comparing the resulting arg lists token-for-token against the Python function's logic — output matches exactly, including the `-ss` audio-offset-only-if->0 branch and the GIF `include_audio=False` override
- [x] Raw frame piping to ffmpeg stdin via `Process` + `Stream` (replaces subprocess pipe in Python) — `Export/VideoRecorder.cs` (`Start`/`WriteFrame`/`Stop`, `RedirectStandardInput` + `ProcessStartInfo.ArgumentList`), wired into `ShaderManager.RenderFrame` (writes the composited frame every frame while `Recorder.IsRecording`, mirroring the Python main loop's per-frame `ffmpeg_process.stdin.write`). Needs `Framebuffer.ReadPixelsRgb()` (new, `glReadPixels` RGB/`UnsignedByte`/pack-alignment 1) to get the raw bytes. **Not flipped** before piping — this deliberately matches the Python source exactly, which pipes `fbo.read()` raw without a vertical flip for recording (only the screenshot path flips); if recorded video turns out upside-down in practice, that's a pre-existing characteristic of the source pipeline, not a porting bug — flag if it needs fixing since ffmpeg's rawvideo convention may want top-down rows
- [x] Audio muxing with offset support (`-ss`, stream mapping) ported 1:1 — part of `FfmpegExporter.BuildArguments`, see above
- [x] Screenshot capture (PNG) from current framebuffer — `Export/ScreenshotService.cs`, using `StbImageWriteSharp` (new dependency; sibling package to the already-referenced `StbImageSharp`, same author/ecosystem, avoids pulling in `System.Drawing.Common`/ImageSharp for one write path). **Does** flip vertically before writing (matches Python's `Image.frombytes(...).transpose(FLIP_TOP_BOTTOM)`). Verified end-to-end against a live GL framebuffer during this session: produced a correctly-oriented, non-corrupt 1.2MB PNG of the actual running shader — visually confirmed right-side-up, not the raw OpenGL bottom-up orientation
- [x] Recording with fixed duration or manual stop, GIF toggle — `Gui/ExportPanel.cs`: Use Fixed Duration checkbox + duration slider, Record as GIF checkbox, Include Audio checkbox, Start/Stop Recording. `VideoRecorder.Start` returning `false` (ffmpeg not found/not on PATH) verified to fail gracefully with a log message and no crash — could not verify a full successful recording end-to-end since this sandbox has no ffmpeg installed (see §12 decision on bundling)
- [x] Export progress bar in UI reflecting ffmpeg encoding time — **Enhancement** (Python version has no progress feedback). Implemented for real, not a synthetic proxy: `FfmpegExporter.BuildArguments(..., reportProgress: true)` adds `-progress pipe:1`; `VideoRecorder` redirects ffmpeg's stdout (stdin is already the raw-frame pipe, so stdout is free) and parses the `key=value` lines ffmpeg emits per frame (`out_time_ms`, `frame`) into `EncodedSeconds`/`EncodedFrames`. `Gui/ExportPanel.cs` shows a real progress bar (fraction of fixed duration) when duration is known, or an elapsed-time/frame counter for open-ended recording. Verified the parser against realistic ffmpeg progress-line output via reflection (ffmpeg itself isn't installed on this sandbox, so the parser was fed the exact line format ffmpeg documents/emits) — `out_time_ms=2000000` + `frame=120` correctly produced `EncodedSeconds=2, EncodedFrames=120`
- [x] Optional hardware-accelerated encode (`h264_nvenc`/`h264_qsv` auto-detect) — **Enhancement**. `Export/FfmpegCapabilities.DetectHardwareEncoder` runs `ffmpeg -hide_banner -encoders` once and checks the output for `h264_nvenc`/`h264_qsv`; `FfmpegExporter.BuildArguments` gained a `hardwareEncoder` parameter that swaps in the correct codec-specific flags (nvenc: `-rc:v vbr -cq 18`; qsv: `-global_quality 18`; both replacing libx264's `-crf` since neither hardware encoder supports it the same way — same target quality, encoder-appropriate knob) instead of naively substituting just the codec name. `Gui/ExportPanel.cs` has a "Use Hardware Encoding (auto-detect)" checkbox that runs detection on first enable and shows what was found (or the libx264 fallback notice). Verified: NVENC/QSV/default argument lists all inspected directly and match the intended flags per encoder; detection fails gracefully (logs and returns `null`) since this sandbox has no ffmpeg installed, confirmed no crash

---

## 8. Media & textures (from `modules/textures.py`, `modules/math_utils.py`)

- [x] `TextureLoader`: load PNG/JPG into GL texture via `StbImageSharp` (replaces PIL-based loader), mipmaps, repeat wrap — `Rendering/TextureLoader.cs` + `Rendering/Texture.cs` (new GL texture wrapper). Also ports the noise-texture fallback from `load_textures()` (`CreateNoiseTexture`). **Wired into the actual render pipeline**, not left as a standalone unused utility: `ShaderManager` now owns a `Channel0Texture` (noise by default), and `RenderPipeline.RenderShaderPass` binds it to unit 0 and sets `iChannel0` every pass (base + each layer) — previously `iChannel0` was declared in `ShaderWrapper` but never actually bound to anything. `Gui/MediaPanel.cs` exposes a path field + Load button (`ShaderManager.LoadChannel0Texture`, falls back to keeping the current texture on failure). Verified against a real 1024×1024 PNG from the source project: loaded with a valid GL handle, no exceptions
- [x] `TextTextureGenerator`: render text to texture (replaces pygame font rendering) — `Rendering/TextTextureGenerator.cs`. **Decision (see §12)**: `System.Drawing.Common`, not `SixLabors.ImageSharp.Drawing`/SkiaSharp — `SixLabors.ImageSharp.Drawing` 3.x turned out to require a paid commercial license (discovered via a build-time license-check failure, not upfront doc reading), and this app is already Windows-only by design (self-contained `win-x64` publish), so `System.Drawing.Common` has no real downside here. Verified end-to-end: rendered "DEMO SHADER" to a texture, drew it to a framebuffer, and screenshotted it — text is correctly centered, right-side-up, and the exact requested color (cyan), confirming both the vertical-flip and BGRA→RGBA channel-swap steps are correct
- [x] `MathUtils`: port helper functions (matrix/vector helpers) to `System.Numerics` (`Matrix4x4`) instead of hand-rolled numpy-style helpers — `Rendering/MathUtils.cs`, direct row-by-row transcription of `math_utils.py`'s 6 functions (`Mat4Perspective`/`Translate`/`Scale`/`RotateX/Y/Z`). **Caught real transcription bugs during verification**: `System.Numerics.Matrix4x4` happens to use the same row-vector convention as the Python numpy arrays here (translation in row 4, not column 4), which made a naive first-pass translation error-prone — the initial draft put translate/rotation values in the wrong row or with flipped signs on 5 of the 6 functions. Caught entirely by direct numeric verification (`Vector4.Transform` against hand-computed expected outputs, e.g. `RotateZ(90°) * (1,0,0,1) ≈ (0,-1,0,1)`) before this was ever wired into rendering code — every function now verified correct
- [ ] Model loading (`cube.obj`) via a small OBJ parser or `AssimpNet` — **Enhancement**: proper 3D model support instead of a single hardcoded cube. Not started this session — the consuming 3D model render pass itself is also still unbuilt (§2.1), so this would exist without a caller; revisit together

---

## 9. GUI / editor (from `modules/gui/*.py`, `main.py` GuiEditor class)

- [x] `Theme`: port cyberpunk ImGui theme (colors/styles) 1:1 to `ImGui.NET` style struct (replaces `theme.py`) — `Gui/Theme.cs`. Note: `render_syntax_highlighting` from the same Python file was **not** ported yet, it belongs with the code-editor tab below
- [x] ImGui wired into the render loop via `Silk.NET.OpenGL.Extensions.ImGui.ImGuiController`, hosted inside `GlWindow` (new `ImGuiRender` event, fires between the 3D scene draw and the ImGui draw-data submit) — `Rendering/GlWindow.cs`. **Pinned dependency**: `ImGui.NET` had to be pinned to `1.89.9.3` (down from the `1.90.9.1` originally scaffolded in §1) because `Silk.NET.OpenGL.Extensions.ImGui 2.21.0`'s `ImGuiController` calls the legacy `ImGuiIOPtr.KeyMap` API that `ImGui.NET 1.90+` removed — see §12
- [ ] Tab: General settings (replaces `tabs.py::draw_general_tab`)
- [ ] Tab: System (replaces `tabs.py::draw_system_tab`)
- [ ] Tab: Popups/dialogs (replaces `tabs.py::draw_popups`)
- [ ] Tab: Code editor for shaders, with syntax highlighting — **Enhancement**: Python version has no syntax highlighting (replaces `tabs_code.py`). **Partial**: `Gui/LiveCodingPanel.cs` (§2.1/§3) already covers plain-text live editing + compile + snippet insertion; syntax highlighting itself (`render_syntax_highlighting` from `theme.py`, noted back in §9's Theme entry) is still not ported
- [ ] Tab: Export (replaces `tabs_export.py`)
- [ ] Tab: Media (image/video/audio **browser** specifically — file pickers/thumbnails for images/video — still unbuilt; the audio **playback controls** part is now covered by the Audio section below, replaces part of `tabs_media.py`)
- [x] Tab: Timeline (replaces `tabs_timeline.py`, see §5 for full detail/limitations) — `Gui/TimelinePanel.cs`, a collapsible "Timeline" section in `EffectsPanel`. Simplified form-based editor, not the graphical drag/resize/scrub UI — see §5
- [x] Tab: Audio (replaces the audio-specific controls in `tabs.py::draw_general_tab`: Audio Reactivity checkbox, volume slider, audio-levels meter — the general tab's non-audio bits like the Timeline-enable checkbox live in the Timeline section instead) — `Gui/AudioPanel.cs`, a collapsible "Audio" section in `EffectsPanel`: Audio Reactivity toggle, volume slider, music file path, Load & Play/Stop, live bass-level progress bar. Verified rendering via screenshot before automated UI testing was halted this session (see §12 incident note) — the panel's correctness was otherwise verified through direct calls to `AudioAnalyzer`/`AudioPlayer`, not through the UI
- [x] Tab: Visuals/effects controls (all `CUSTOM_*` sliders, replaces `tabs_visuals.py`) — `Gui/EffectsPanel.cs`, single ImGui window grouped Color & Light / Color Grading / Geometry / Glitch & Retro / Artistic / Motion / Particle System, plus a shader picker (combo + Previous/Next) reading `ShaderManager.ShaderNames`; verified visually (screenshot) rendering over the live shader background with the theme applied. Timeline/media/export tabs below are still separate, unstarted tabs
- [x] Tab: Layers/effect stack (replaces `tabs_visuals.py::draw_layers_tab`) — `Gui/LayersPanel.cs`, a collapsible "Layers" section inside the same `EffectsPanel` window (Add/Save/Load buttons, per-layer enabled checkbox + shader combo + blend-mode combo + opacity slider, Up/Down reorder buttons, collapsing-header "X" to remove). **Simplification**: reorder uses Up/Down arrow buttons instead of the Python version's ImGui drag-and-drop payload — functionally equivalent, avoids the extra marshalling complexity of drag-drop payloads in ImGui.NET; revisit only if drag-and-drop is specifically requested. Verified visually: Add Layer creates "Layer 1: TestPattern" and expands correctly, no crash
- [x] Shared UI utils/widgets (replaces `tabs_utils.py`) — `Gui/UiUtils.cs`, `SliderWithReset` (slider + "R" button resetting to the same neutral default used by the Python `slider_with_reset` calls, not the `settings.py` initial value — those two are deliberately different concepts). `EffectsPanel` now uses it for every float slider
- [x] Tab: 3D Model (new, not itemized separately in the original checklist — replaces `ModelMixin`'s UI-facing bits) — `Gui/Model3DPanel.cs`: Show Model toggle, OBJ/texture path + Load buttons, position/rotation/scale/auto-rotate/light-direction sliders, wireframe toggle + color
- [x] Tab: Presets (replaces the preset-related bits of `tabs_visuals.py::draw_presets_tab`) — `Gui/PresetsPanel.cs`: name + Save, Randomize, per-preset Apply/Delete list
- [x] Tab: Live Coding (new — replaces `is_live_coding_active`/`live_shader_program` toggle + editor bits of `tabs_code.py`, minus syntax highlighting, see §2.1) — `Gui/LiveCodingPanel.cs`
- [x] Tab: Window (new — Open/Close for the secondary output window, see §2.1) — `Gui/WindowPanel.cs`
- [ ] Project mixin → `ProjectService` (new/open/save project state)
- [ ] Automation mixin → part of `TimelineEngine` (§5)
- [ ] Media mixin → `MediaService`
- [ ] Shader editor mixin → shader code editor integration (§9 code tab)
- [ ] Recorder mixin → `RecorderService` (§7)
- [x] Window mixin → `WindowService` (fullscreen toggle, multi-monitor output) — **partial**: multi-monitor secondary output is done (`SecondaryWindow`, see §2.1); fullscreen toggle for the primary window is not wired to any UI control yet (`GlWindow` already accepts a `fullscreen` constructor flag, just not exposed at runtime)
- [ ] Keyboard shortcuts parity (toggle GUI, fullscreen, screenshot, etc.)
- [ ] Docking layout via ImGui docking branch, replacing static `imgui.ini` layout — **Enhancement**

---

## 10. Settings & persistence

- [x] `AppSettings` class: 1:1 port of every field in `settings/settings.py` (`ShaderSettings`), strongly typed, English-named — `Settings/AppSettings.cs`. `WindowWidth`/`WindowHeight`/`ShaderSwitchInterval`/`MusicFile`/`MusicVolume`/`AudioReactive`/`AutoSaveSettings` are top-level fields; all `CUSTOM_*` fields live on the embedded `EffectParams Effects` (§3/§4) rather than being duplicated — `EffectParams.CopyFrom` bridges the persisted copy and the live `ShaderManager.Effects` instance
- [x] JSON-based settings persistence (`System.Text.Json`), replacing implicit Python class defaults — `Settings/SettingsService.cs` (`Load`/`Save`, `IncludeFields = true` since `EffectParams`/`Vector2/3/4` expose public fields, not properties). Wired into `Program.cs`: loads `settings.json` before window creation (drives initial window size), applies `Effects` once the engine exists, and `EffectsPanel` has "Save Settings"/"Load Settings" buttons. Round-tripped and verified: saved JSON matches `settings.py` defaults field-for-field, reloads without exceptions
- [x] `layers.json` model load/save ported — `Settings/LayerPersistence.cs`, wired into `Gui/LayersPanel.cs`'s Save/Load buttons. **Deviation**: JSON keys are PascalCase (`ShaderName`/`BlendMode`/`Opacity`/`Enabled`) to match this project's own `System.Text.Json` default convention, not the Python file's snake_case (`shader_name`/...) — reading an old Python `layers.json` directly is out of scope here and belongs with the §12/§10 migration-tool item
- [x] Auto-save settings toggle (`AUTO_SAVE_SETTINGS`) ported — `AppSettings.AutoSaveSettings`, checkbox in `EffectsPanel`, and `Program.cs`'s `window.Closing` handler saves to `settings.json` when it's true (verified via a graceful `WM_CLOSE`, not a force-kill, since only a clean shutdown runs the handler)
- [x] Settings migration tool: one-off importer that reads the old Python project's JSON/db files into the new format — **Enhancement**, `Migration/` (new namespace): `PythonLiteralParser.cs` (shared value parsing: floats, ints, bools, quoted strings, `(a, b, c)` tuples, and `UPPER_SNAKE_CASE` → `PascalCase` for reflection field lookup), `SettingsMigrator.cs` (parses `settings/settings.py` line-by-line — deliberately not a full Python parser, just enough for this file's flat `NAME = literal` shape — and reflects `CUSTOM_*` fields onto `EffectParams` the same way `TimelineEngine.ApplyEffects` and `main.py`'s own `apply_timeline_effects` do), `LayersMigrator.cs` (reads the *old* snake_case `layers.json`), `TimelineMigrator.cs` (reads `timeline.db` via `Microsoft.Data.Sqlite` — added as a migration-only dependency, deliberately not used for this project's own ongoing timeline persistence, see the JSON-not-SQLite decision in §12), and `ProjectMigrator.cs` orchestrating all three with per-step try/catch so one missing/broken legacy file doesn't block the others. `Gui/MigrationPanel.cs` exposes it as a "Migration" section: a folder path field, a Migrate button, and a scrollable log; on success it applies the result to the live session (`ShaderManager.Effects`/`Layers`, `TimelineEngine`) *and* writes `settings.json`/`layers.json`/`timeline.json` immediately. **Verified against the real, actual legacy project** at `C:\Users\Patrick\Desktop\OPENGL_SHADERSDEMO` (not synthetic test data): 59/59 `CUSTOM_*` fields migrated with 0 skipped and exact value matches, 1 layer migrated correctly, and — reading the real `timeline.db` SQLite file — 41 clips + 2 markers migrated with correct types/resources/params. Also verified the graceful-skip path (nonexistent folder → three clear "not found, skipped" messages, no exception)

---

## 11. Packaging & distribution

- [x] `dotnet publish -r win-x64 --self-contained -p:PublishSingleFile=true` producing `ShaderDemo.exe` — **found and fixed a real bug while verifying this** (2026-07-08), not just ran the command and assumed it worked: the published exe crashed instantly with `PlatformNotSupportedException: Couldn't find a suitable window platform. (GlfwPlatform - not applicable, SdlPlatform - not applicable)` on every launch, while the ordinary `dotnet build` output worked fine. Root cause: `<IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>` bundles `glfw3.dll`/`cimgui.dll`/`SDL2.dll`/`e_sqlite3.dll` *inside* the single exe for extraction to a temp dir at first run, but Silk.NET's own native-library resolver (confirmed via its troubleshooting doc, which the exception links to) looks in the program's output directory, not wherever `AppHostExtractor` unpacks to — so it never found them. **Fix**: `ShaderDemo.App.csproj`, flipped that setting to `false`, which makes `dotnet publish` copy the native DLLs as loose files next to `ShaderDemo.exe` instead of embedding them (a "mostly single file" compromise: one `.exe` + 4 small native DLLs + `shaders/`, not a literal single file — but a truly-embedded native GL/audio stack isn't achievable here without patching Silk.NET's loader). Verified end-to-end 3 ways: direct `dotnet publish` + run, `build.ps1` (the project's actual documented build entry point) + run from its `publish/` output, and confirmed the plain `dotnet build` dev loop is unaffected by the csproj change — all three launch cleanly with zero stderr and load all 32/33 shaders
- [x] Embed icon, version info, copyright metadata — icon was already wired (§1); added `<Version>`/`<AssemblyVersion>`/`<FileVersion>`/`<Description>` to both `.csproj` files (previously missing — `Copyright`/`Product`/`Company` were set but the version fields weren't). Verified against the actual published binary's Win32 version resource via `(Get-Item ShaderDemo.exe).VersionInfo`: `FileVersion=0.1.0.0`, `ProductVersion=0.1.0`, `CompanyName=Patrick JAILLET`, `LegalCopyright=Copyright (c) 2026 Patrick JAILLET` all correct. **Minor known gap**: `FileDescription` shows the product name ("ShaderDemo") instead of the `<Description>` text — the .NET SDK's `Description`→Win32-resource mapping didn't pick it up as expected; not chased further, cosmetic only
- [x] Bundle `ffmpeg.exe` or document it as an external dependency — already decided in the §7 session (external dependency, not bundled) — see §12
- [x] Installer (optional): `Inno Setup` — **Enhancement**, Python version only produced a raw PyInstaller exe. `installer/ShaderDemo.iss` (packages the `publish/` output: exe + 4 native DLLs + `shaders/`, Start Menu + optional desktop shortcut, launch-after-install checkbox, `[UninstallDelete]` for the runtime-generated `settings.json`/`layers.json`/`timeline.json`/`presets.json`/`imgui.ini`) + `installer/build-installer.ps1` (runs `build.ps1` then `ISCC.exe`, matching the existing single-entry-point build-script pattern). **Design choice**: `PrivilegesRequired=lowest` + `PrivilegesRequiredOverridesAllowed=dialog commandline` instead of the Inno Setup default (`admin`) — this is a single-user demo/visualizer tool with no shared-system footprint, so forcing a UAC prompt and a Program Files install had no real benefit; per-user install to the local-app equivalent of `{autopf}` avoids it entirely while still allowing an admin/all-users install via the wizard or `/ALLUSERS` if wanted. **MSIX not attempted** — Inno Setup fully covers the "optional installer" need already and MSIX's added complexity (packaging identity, signing) isn't justified for an unsigned single-user tool. Verified as a real install, not just "the compiler didn't error": silent-installed to a scratch directory (`/VERYSILENT /DIR=...`, no UAC prompt as intended), ran the installed `ShaderDemo.exe` from that location (32/33 shaders loaded, clean exit), then silently uninstalled and confirmed the install directory was fully removed with nothing left behind
- [x] Remove all PyInstaller/`compile.py`/`.aiexclude` artifacts, replaced by `build.ps1` / `dotnet publish` — trivially already true: this is a from-scratch C# solution, none of those Python build artifacts were ever present in this repo to begin with (confirmed: no `.spec`, `compile.py`, or `.aiexclude` anywhere under the project root)

---

## 12. Decisions log

*(append one line per decision as the port progresses — do not delete history)*

- [x] Timeline storage format: **JSON**, not SQLite (2026-07-08) — this project's entire persistence layer (`settings.json`, `layers.json`) is already `System.Text.Json`; adding `Microsoft.Data.Sqlite` as a second persistence mechanism for one more small, list-shaped data structure (clips + markers) would be extra dependency weight and a second code path for no real benefit at this project's scale. `Timeline/TimelinePersistence.cs` follows the same pattern as `LayerPersistence.cs`. Not attempting to read the Python project's existing `timeline.db` — that conversion is §10's separate "migration tool" Enhancement, not in scope here
- [x] ffmpeg bundling vs external dependency — **decision: external dependency, documented, not bundled** (2026-07-08) — `VideoRecorder.FfmpegPath` defaults to `"ffmpeg"` (resolved via `PATH`) and is overridable. Bundling `ffmpeg.exe` means either shipping a multi-hundred-MB binary inside the single-file publish (works against the "no PyInstaller-style bloat" goal in §0) or a separate download/installer step — neither is justified before there's a packaging story (§11) to hang it on. `VideoRecorder.Start` fails gracefully (returns `false`, logs the reason) when ffmpeg isn't found, verified on this sandbox which genuinely has no ffmpeg installed. Revisit if §11 packaging wants a fully self-contained "no external installs" experience
- [x] Text rendering library for text-texture generation — decided: `System.Drawing.Common`, see the full entry below (this line was left stale after that decision — fixed)
- [x] NuGet package versions pinned at scaffold time: `Silk.NET.* 2.21.0`, `ImGui.NET 1.90.9.1`, `StbImageSharp 2.30.15`, `NAudio 2.2.1` — **bump these on first real `dotnet restore` if newer stable versions exist**
- [x] Target: `net8.0`, `win-x64`, single-file self-contained publish
- [x] **Environment note**: this sandbox has no .NET SDK installed, so the scaffold below has not been built/restored yet. First action on a machine with `dotnet` installed: run `dotnet restore` then `dotnet build` to confirm the scaffold compiles before adding more code.
- [x] `dotnet build` confirmed working (2026-07-08): SDK 8.0.100 not present on this machine, only 10.0.301 — `global.json` `rollForward` changed from `latestMinor` to `latestMajor` so SDK 10 builds the `net8.0`-targeted projects. Also added `<AllowUnsafeBlocks>true</AllowUnsafeBlocks>` to `ShaderDemo.Core.csproj` (required by pointer usage in `Framebuffer.cs`/`FullscreenQuad.cs`/`GlContext.cs`, was missing from the original scaffold). Solution now builds and the test-pattern app runs with no GL shader compile errors.
- [x] `shaders/` folder added at repo root (2026-07-08), copied as-is from the Python project's `shaders/*.hlsl` (33 files) and wired via `<None Include>`/`CopyToOutputDirectory` in `ShaderDemo.App.csproj` so they land next to the built exe.
- [x] Non-ASCII bytes (French accented comments) in several `.hlsl` files corrupted the native fragment shader source when marshalled to the GL driver, producing misleading `unexpected $end` link errors — `ShaderLoader` now strips any character ≥ U+0080 to a space before conversion. This is also the practical enforcement point for the §0 English-only rule on shader content going forward.
- [x] `ImGui.NET` pinned to `1.89.9.3` (2026-07-08), not the `1.90.9.1` originally scaffolded in §1 — `Silk.NET.OpenGL.Extensions.ImGui 2.21.0` (added this session to host ImGui inside the Silk.NET/OpenGL render loop) depends on the pre-1.90 `ImGuiIOPtr.KeyMap` input API that `ImGui.NET` removed in 1.90; using 1.90.9.1 throws `MissingMethodException` at `ImGuiController` construction. Revisit this pin if a newer `Silk.NET.OpenGL.Extensions.ImGui` targeting current ImGui.NET ships.
- [x] `TimelineEngine.ApplyEffects` maps a clip's `Resource` string (e.g. `"rgb_split"`) to an `EffectParams` field (`RgbSplit`) via `System.Reflection` + snake_case→PascalCase conversion, deliberately mirroring the Python original's own `"CUSTOM_" + resource.upper()` + `setattr` dynamic dispatch rather than writing a static switch — new `EffectParams` fields need no corresponding change here, same as the Python version needed no `if/elif` change when a new `CUSTOM_*` field was added. Verified: builds clean, UI smoke-tested (panel renders, Add-clip form accepts input, no crash across repeated interaction, shader Previous/Next confirmed working live); the interpolation/reflection math itself was verified by code review against `main.py`'s `calculate_interpolated_value`/`apply_timeline_effects`, not by a full click-driven "add a clip, enable, watch a slider move" scenario — flag if that end-to-end path needs dedicated testing before relying on it.
- [x] Audio verification method changed after an incident (2026-07-08): simulated-input UI screenshots (mouse coordinates + `SendKeys`) were being used to smoke-test new ImGui panels. During the §6 Audio panel test, the app window lost foreground focus to a browser without that being noticed between steps, and the simulated clicks/keystrokes intended for the ImGui text field landed on a live Shadertoy.com comment box instead, resulting in an unintended comment actually being posted to a public page under the user's account. The comment text was cleared from the input box immediately after, but had already been submitted before that. **Going forward**: prefer direct calls into the library (as done for `AudioAnalyzer`/`AudioPlayer` in this same session — real file, real playback, asserted on return values/state, zero UI risk) over coordinate-based simulated mouse/keyboard input against the actual desktop; only use simulated GUI input again if explicitly requested, and if so, verify window focus before every single action rather than assuming it holds across steps.
- [x] **Bug found and fixed (2026-07-08), unrelated to §7 but discovered while verifying it**: the app crashed on every graceful window close with a fatal CLR interop error (`Marshal.GetDelegateForFunctionPointerInternal` inside `Silk.NET.Input.Glfw.GlfwEvents.Dispose()`, called from `GlWindow.Dispose()`). Reproduced on a completely vanilla close (no export/recording code involved at all) — pre-existing since §9 wired up `Silk.NET.Input`/ImGui, just not previously exercised by a graceful-shutdown test. Root cause: `GlWindow.Dispose()` was explicitly disposing the `IInputContext` after `ImGuiController` — `ImGuiController` already tears down its own GLFW callback subscriptions internally, so the explicit `_inputContext.Dispose()` afterward was a double-unregistration hitting an invalid callback pointer. **Fix**: removed the explicit `_inputContext.Dispose()` call from `GlWindow.Dispose()` (`Rendering/GlWindow.cs`) — the window's own disposal is sufficient. Verified fixed across 3 consecutive clean-close runs with no stderr output, where it crashed 100% of the time before.
- [x] Text rendering library: **decision made — `System.Drawing.Common`** (2026-07-08), resolving the §8 "pending decision". First attempt was `SixLabors.ImageSharp.Drawing`, which built fine locally but failed at `dotnet build` with a hard license-check error (`SixLabors.ImageSharp.Drawing.targets`: "No Six Labors license found... obtain a license from sixlabors.com/pricing") — version 3.x turned out to require a paid commercial license, not discoverable from the NuGet listing alone. Switched to `System.Drawing.Common`: since this project is already Windows-only by design (§0/§11: self-contained `win-x64` publish, `NAudio.WaveOutEvent` is already a Windows-only API in practice), there's no real cross-platform cost to accept. To keep this clean under a plain `net8.0` TFM (not `net8.0-windows`), `TextTextureGenerator` is annotated `[SupportedOSPlatform("windows")]` and `ShaderDemo.App`'s `Program.cs` carries `[assembly: SupportedOSPlatform("windows")]` — this suppresses the `CA1416` platform-compatibility warning correctly (the analyzer needs the *caller* annotated too, not just the callee) without changing the documented TFM decision.

---

## 13. Cross-cutting improvements (beyond straight port)

- [ ] Replace print-based logging with a proper `ILogger` (console + file sink), English messages only
- [ ] Unit tests project `ShaderDemo.Tests` for: automation curve interpolation, ffmpeg command builder, HLSL→GLSL converter, settings serialization
- [ ] Config validation on load (clamp out-of-range effect values instead of silently accepting garbage)
- [ ] Crash handler with log dump instead of raw Python traceback in a console window
- [ ] Performance: instanced rendering for particle system, GPU timer queries shown in a debug overlay
- [x] `ShaderDemo.App/FullFeatureTest.cs` (2026-07-08): a code-only integration test, run via `ShaderDemo.exe --test-full [audioPath] [modelPath]`, that exercises bloom + particles + a real 3D model + a shader transition + datamosh→feedback→motion-blur switching + live-shader compilation + a timeline text overlay + full audio spectrum/waveform analysis + the secondary preview window, all in one ~90-frame run, ending with a screenshot. Kept intentionally (not deleted like the other sessions' throwaway `--test-*` blocks) because §2.1's render pipeline is now complex enough, and GUI-click testing risky enough (see the Shadertoy incident above), that a repeatable non-interactive regression check has real ongoing value. Not a substitute for the unit-test project item above — it's a smoke test, not assertions

---

## 14. Final acceptance checklist

- [ ] All shaders from `shaders/` render identically to the Python version (visual diff pass)
- [ ] All `CUSTOM_*` parameters produce the same visual effect at the same slider value
- [ ] Timeline project files from the Python version load correctly (via migration tool)
- [ ] Video/GIF export produces files with matching resolution/fps/quality settings
- [ ] No non-English string remains anywhere in UI, logs, or shader names
- [ ] No comment (other than the copyright header line) remains anywhere in the C# codebase
- [ ] Single-file `.exe` runs standalone on a clean Windows machine
